def last_digit(lst):
    number = lst[0]** (list[1]**list[2])
    return number[-1]

last_digit(1,1,1)

'''def square_sums(num):
    lista = list(range(1,int(num)+1,1)) #creates the list

    todosfunfa = False

    for elemento in range(0,len(lista)+1):
        print(elemento) # linha de controle numero de iterações

        e = elemento[0] + elemento[-1]
        print(e) #linha de contole somas dos elementos

    # elemento contém um numero que tem raiz redonda?
        if e**(1/2)%2 != 1:
            todosfunfa = False
        else:
            todosfunfa = True
    return todosfunfa
'''

'''
    from random import shuffle
    parar = False
    shuffle(lista)

    while parar == False:
        parar = True

        for c in range(0, len(lista)-1):
            if (lista[c]+lista[c+1])**(1/2) % 2 == 1:
                lista = lista
            else:
                listanova = 
                parar = False
    return lista
'''


#print(square_sums(15))
